 
# These requirements are for the Python that is run in the
# neuro_san/service container.
#
# Requirements for tests, code style and other build stuff reside in
# requirements-build.txt

# Do the source-available stuff first to fast-fail if access is not right

# Public repos
git+https://github.com/leaf-ai/leaf-common.git@1.2.20
git+https://github.com/leaf-ai/leaf-server-common.git@0.1.17
